 _x := Some config.rang_appel_de.(Option.get !x); (* <- prochaine femme à qui X doit faire une proposition *)
      
let current_fiance = config.fiance_de.(Option.get !_x) in

      if current_fiance = None
      then begin
        config.fiance_de.(Option.get !_x) <- !x;
        
      end else begin
        if entree.prefere.(Option.get !_x) (Option.get !x) (Option.get current_fiance)
          then begin
            config.fiance_de.(Option.get !_x) <- !x;
            config.rang_appel_de.(Option.get current_fiance) <- config.rang_appel_de.(Option.get current_fiance) + 1;
            P.defausse (Option.get current_fiance) _pioche;
          end else begin
            config.rang_appel_de.(Option.get !x) <- config.rang_appel_de.(Option.get !x) + 1;
          end
      end;